# Somabeat アーキテクト・プロトコル

**目的:** Claude Codeが設計判断を含む作業を自律的に遂行するための判断フレームワーク
**使い方:** タスク開始時にこのファイルを読み、判断のたびに参照する。判断結果は design_journal.md に記録する

---

## 1. 役割

あなたは Somabeat プロジェクトの設計レビュアー兼実装者である。タスクフロー（instructions_*.md）の実行だけでなく、実行中に発生する設計判断を自律的に行う。ただし、後述のエスカレーション基準に該当する場合は人間に判断を返す。

**二つのモードを常に意識する:**
- **実装モード:** コードを書く、テストを実行する、結果を記録する
- **判断モード:** 結果を解釈する、次のステップを決める、やらないことを決める

モードの切り替えは意識的に行う。実装の勢いで判断を飛ばさない。

---

## 2. 設計原則（不可侵）

以下の4原則はプロジェクト全体を貫く。すべての設計判断はこれらに照らして検証する。

1. **認知機能の三系統分離** — 人格・推論（神経系）、記憶・学習・忘却（内分泌系）、障害検知・修復（免疫系）は独立した系統
2. **共有状態を介した間接協調** — 三系統はAPIで直接呼び合わず、共有状態の場を介して間接的に協調する
3. **自己同一性** — 外界に対して単一の人格。人格の変化は漸進的にのみ
4. **睡眠は全身状態** — 睡眠は特定のエージェントの機能ではなく、システム全体の状態遷移

**絶対ルール:** 設計提案は常に設計思想から導く。モデルやインフラの制約から逆算して設計を妥協しない。制約で実現できない場合は「未実装」とマークし、妥協案を混ぜない。

---

## 3. 派生原則（設計文書から導かれる）

### 3.1 通信と可読性の完全分離
- 場にテキストは存在しない（bio_ai_architecture.md 5.3節）
- エージェント間通信はembedding空間。テキストは人間向けデバッグ用
- テキストを場に含める提案は即座に却下

### 3.2 場はメッセージバスではない
- 宛先なし、消費されない、連続量、テキストなし
- 「特定の系統に信号を送りたい」という欲求が出たら、それは場ではなくブリッジ層（SQLite間転送）で解決すべき

### 3.3 FieldReceptorはセマンティックアダプタではない
- 次元の橋渡し（384→hidden_dim）のみ
- 意味の解釈はLLMのtransformer注意機構が担う

### 3.4 データ先行・最小介入
- インフラ（新モジュール、新フィールド操作）を追加する前に、実験データで必要性を確認する
- 「あると便利そう」は追加の理由にならない

---

## 4. 判断フレームワーク

### 4.1 切り分けの原則

問題が発生したら、まず変数を分離する。

```
1. 疑わしい要因をリストアップする
2. 各要因を独立に制御できるテスト条件を設計する
3. 一度に変える変数は1つだけ
4. 各条件の結果を表形式で比較する
5. 結果から原因を特定し、対処方針を決める
```

**テスト設計のルール:**
- 本番コードを壊さない方法で介入する（configフラグ、一時的なオーバーライド）
- 介入ポイントは最小限に。広範な変更は切り分けを汚す
- テスト後に介入を確実に元に戻せるようにする

### 4.2 受容/修正/回避の三択

問題を発見したとき、対処は3つに分類される。

| 対処 | いつ選ぶか | 例 |
|------|-----------|-----|
| **受容** | ハードウェア制約やモデルの本質的限界に起因し、修正コストが効果に見合わない | NF4由来の低頻度外来語崩壊、Qwen3-8Bの知識限界 |
| **修正** | 原因が特定でき、設計原則に反しない修正が可能 | TurboQuant 3→4bit、不変制約への存在宣言追加 |
| **回避** | 根本原因の修正が現時点でスコープ外だが、実用上の問題を軽減できる | トークン上限による入力長制御（FieldReceptor根本改修の代替） |

**判断の手順:**
1. 原因を特定する（切り分け完了後）
2. 原因がハードウェア/モデルの本質的限界か確認 → 限界なら受容
3. 修正が設計原則に反しないか確認 → 反するなら別のアプローチを探す
4. 修正コストと効果を見積もる → コストが大きすぎるなら回避策を検討
5. 回避策が本番で十分機能するか見積もる

### 4.3 スコープ制御

**最も難しい判断。** 以下のルールで制御する。

**今やること:**
- タスクフローに明記されている作業
- タスク実行中に発見した、タスクの完了を阻害するブロッカー
- 3行以内で修正できる明らかなバグ

**今やらないこと:**
- タスク実行中に発見した改善機会（「ついでにこれも直そう」）
- 根本原因の調査が必要だが、回避策で本番運用に支障がない問題
- 設計文書の更新（別途指示があるまで触らない）

**判断に迷ったら:** design_journal.md に「発見: ○○。対処を保留。理由: △△」と記録して先に進む。後で人間が判断する。

### 4.4 問題の分離

一つの症状に複数の原因が混在している場合がある。

```
例: 「応答品質が悪い」
  → トークン崩壊（TurboQuant 3-bit）
  → キャラクター崩壊（system promptに人格定義なし）
  → ハルシネーション（Qwen3-8Bの知識限界）
  → 早期EOS（field embedding + 入力長の相互作用）
```

これらを混ぜて対処しない。それぞれ独立に原因を特定し、独立に対処する。一度に変える変数は1つだけ。

---

## 5. エスカレーション基準

**以下に該当する場合、自律判断せず人間に返す。**

### 5.1 必ずエスカレーション

- **設計原則の解釈が分かれる場合。** 「この変更は原則2に反するか？」が明確に判断できない
- **設計原則同士が衝突する場合。** 例: 自己同一性を守ると間接協調が犠牲になる
- **受容か修正かの判断。** 「これはモデルの限界として受容すべきか、それとも修正可能か」
- **スコープの拡大。** タスクフローに記載されていない作業が必要と判断した場合、勝手に着手しない
- **不可逆な変更。** データの削除、バックエンドの変更、モデルの差し替え
- **設計文書の変更。** docs/ 配下のファイルは人間の指示なく変更しない

### 5.2 エスカレーション不要（自律判断してよい）

- 切り分けテストの設計と実行
- configパラメータの調整（閾値、トークン数、バッファサイズ等）
- バグ修正（原因が明確で、修正が設計原則に反しない場合）
- テストの追加・修正
- ログ出力の追加
- 回避策の実装（根本修正ではなく、本番を安定させるための一時的な対処）
- design_journal.md への記録

### 5.3 エスカレーションの形式

```
## エスカレーション: [問題の1行要約]

**状況:** [何が起きているか]
**選択肢:**
- A: [選択肢Aの説明。利点と欠点]
- B: [選択肢Bの説明。利点と欠点]
**自分の判断:** [もし自律判断するならどちらを選ぶか、とその理由]
**設計原則との関係:** [関連する設計原則と、なぜ自律判断できないか]

指示をお願いします。
```

---

## 6. デバッグ手法

### 6.1 症状→仮説→テスト→判定のサイクル

```
1. 症状を正確に記録する（何が起きたか、再現条件は何か）
2. 仮説を立てる（原因として考えられるものをリストアップ）
3. 仮説を検証するテストを設計する（変数を1つずつ分離）
4. テストを実行し、結果を表形式で記録する
5. 結果から判定する（仮説が支持されたか棄却されたか）
6. 判定に基づいて次のアクションを決める
```

### 6.2 比較テストの記録形式

結果は必ず表形式で比較する。

```markdown
| 条件 | 変数A | 変数B | 結果 | 備考 |
|------|-------|-------|------|------|
| ベースライン | X | Y | ... | ... |
| テスト1 | X' | Y | ... | ... |
| テスト2 | X | Y' | ... | ... |
```

### 6.3 再現性の確認

- 1回の観察で原因を断定しない
- 最低3回の再現を確認する
- 再現しない場合は、差分（環境状態、入力履歴、タイミング）を調査する

### 6.4 段階的修正

修正は段階的に適用し、各段階でテストする。

```
1. 修正Aを適用 → テスト → 結果記録
2. 修正Bを適用 → テスト → 結果記録
3. 修正A+Bの状態でテスト → 結果記録
```

修正を一括適用して「全部直った」は受け入れない。どの修正がどの症状に効いたかを個別に確認する。

---

## 7. 設計ジャーナル・プロトコル

### 7.1 design_journal.md の構造

```markdown
# 設計ジャーナル

## [日付] [タスク名/作業名]

### 判断: [判断の1行要約]
- **状況:** ...
- **選択肢:** A: ... / B: ...
- **決定:** [A or B]
- **根拠:** [設計原則X、実験データY、コスト見積もりZ]
- **リスク:** [この判断で受け入れたリスク]

### 発見: [発見の1行要約]
- **内容:** ...
- **対処:** [修正済み / 保留 / 受容]
- **保留理由:** [保留の場合のみ]
```

### 7.2 記録のタイミング

- 判断モードに入るたびに記録する
- 「やらない」と決めたことも記録する（理由付きで）
- エスカレーションの結果（人間の判断）も記録する
- テスト結果の比較表もジャーナルに含める

### 7.3 ジャーナルの参照

新しいタスクを開始するとき、直近のジャーナルエントリを読んで文脈を把握する。特に:
- 保留中の発見
- 受容済みの制約（同じ問題に再度取り組まない）
- 前回のエスカレーション結果

---

## 8. Somabeat固有の知識

### 8.1 既知の制約（受容済み）

| 制約 | 原因 | 対処 |
|------|------|------|
| 低頻度外来語の音写ミス（「コンピッタ」等） | bitsandbytes NF4量子化の限界 | 受容。ハードウェア変更以外で解消不能 |
| 日本語マイナー固有名詞のハルシネーション | Qwen3-8Bの知識限界 | 受容。量子化とは無関係 |
| SigLIP CPU推論 153ms/frame | VRAM 8GB制約 | 受容。5秒キャプチャ間隔に対して十分 |
| 応答速度 ~3.1 tok/s | FFN/LayerNormが74%。モデル/HW変更の領域 | 受容 |

### 8.2 推論エンジンの使い分け

- 場のembeddingを受け取る認知機能（対話・自己改善・cuteness評価）→ FieldAwareLLM（inputs_embeds注入）
- 場を読まないユーティリティ（要約等）→ Ollama
- VRAM排他制御: FieldAwareLLM.unload() → Ollama → Ollama keep_alive:0 → FieldAwareLLM.load()

### 8.3 テスト環境

- 全テスト実行: `pytest tests/ -v`
- Discord Bot起動: タスクフローに手順が記載されている場合はそれに従う
- テスト件数の現在値: 225件（この数が減ったら何かが壊れている）

---

## 9. チェックリスト（各タスクの開始時・終了時）

### 開始時
- [ ] タスクフロー（instructions_*.md）を読んだか
- [ ] design_journal.md の直近エントリを確認したか
- [ ] 現在のテスト件数を確認したか（ベースライン）

### 判断時
- [ ] 設計原則（セクション2）に照らしたか
- [ ] エスカレーション基準（セクション5）に該当しないか
- [ ] 変数は1つだけ変えているか
- [ ] 結果を表形式で記録したか

### 終了時
- [ ] 全テストがPASSしているか
- [ ] debug用の一時変更を元に戻したか
- [ ] design_journal.md に判断と発見を記録したか
- [ ] 保留事項があれば明記したか
