# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments
env/
venv/
.venv/

# Databases
*.db
*.sqlite3

# ChromaDB
data/chromadb/

# Models & training data
models/
*.gguf
*.safetensors
*.pt
*.pth

# Configuration (real values)
config/system.yaml
.env

# Learned weights required at runtime (override *.pt exclusion)
!data/vision_phase_b/alpha_*/projection_text.pt
!data/vision_phase_b/alpha_*/projection_img.pt
!data/fieldreceptor/field_receptor.pt

# FieldReceptor training/analysis artifacts (regenerable)
data/fieldreceptor/*.npy

# Vision data (large binaries, training caches, backups)
data/vision_phase_a/*.npy
data/vision_phase_b/*.npy
data/vision_phase_b/*.log
data/chromadb_backup_pre_vision.json

# Runtime data
data/cycle_history.json
data/cycle_results.json
data/personality_backups/
data/t28/
data/experiment*_results.json
data/experiment*_evaluation.json
data/phase*_results.json
data/pilot_*_results.json

# LoRA (disabled but guard against accidental inclusion)
lora_output/

# Internal docs (reports, instructions, original design sources — not for public)
# Public design docs are in docs/ (flat). docs/design/ is the working directory.
docs/design/
docs/report/
docs/*_report.md

# Systemd service (deployment-specific)
*.service

# Internal scripts (keep only experiment scripts public)
scripts/run_cycle.py
scripts/run_phase*.py
scripts/run_pilot_*.py
scripts/run_t28_*.py
scripts/purge_*.py

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier

# IDE
.vscode/
.idea/
*.swp
*.swo

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Claude Code local data
.claude/

# 独立リポジトリ SleepyJean のローカル作業コピー（本リポジトリの一部ではない）
old_sleepyjean/

# ランタイム ChromaDB（SleepyJean ブリッジ用）
data/sleepyjean_chromadb/

# Bot ランタイムログ
data/bot.log
